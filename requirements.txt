streamlit>=1.12.0
pandas>=1.3.0
numpy>=1.21.0
opencv-python>=4.5.0
pillow>=8.0.0
scikit-learn>=1.0.0
scikit-image>=0.18.0
matplotlib>=3.4.0
seaborn>=0.11.0
joblib>=1.0.0
openpyxl>=3.0.0
scipy>=1.7.0

# Deep Learning — required for Grad-CAM (MobileNetV2) visualization
# PyTorch is used instead of TensorFlow because TF does not yet support Python 3.14
# Install CPU-only build (no GPU required):
torch>=2.0.0
torchvision>=0.15.0
